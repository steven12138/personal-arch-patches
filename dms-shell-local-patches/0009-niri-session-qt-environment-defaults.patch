--- a/usr/bin/niri-session
+++ b/usr/bin/niri-session
@@ -33,6 +33,14 @@
     # Reset failed state of all user units.
     systemctl --user reset-failed
 
+    # DMS launches some applications through its daemon and others through
+    # Quickshell. Define one niri-only Qt environment before either path is
+    # started so Files, launchers, D-Bus activation, and systemd agree.
+    : "${QT_QPA_PLATFORM:=wayland;xcb}"
+    : "${QT_QPA_PLATFORMTHEME:=gtk3}"
+    : "${QT_QPA_PLATFORMTHEME_QT6:=gtk3}"
+    export QT_QPA_PLATFORM QT_QPA_PLATFORMTHEME QT_QPA_PLATFORMTHEME_QT6
+
     # Import the login manager environment. systemd warns when this command
     # is called without a variable list, so pass the names actually present in
     # this login environment. Keep the list explicit so values containing
